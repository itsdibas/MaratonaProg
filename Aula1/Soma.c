#include <stdio.h>
#include <stdlib.h>

int main(){
    // Lendo a entrada do exercício
	int X, Y, soma;
	scanf("%d",&X);
	scanf("%d",&Y);
	soma = X+Y;
	
	printf("%d", soma);
    // Seu código vai aqui
	return 0;
}